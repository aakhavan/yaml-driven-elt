# Used locally for reference; do not commit real secrets.
AIRFLOW_UID=50000
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=minio123
POSTGRES_META_USER=airflow
POSTGRES_META_PASSWORD=airflow
POSTGRES_META_DB=airflow
POSTGRES_W_USER=warehouse
POSTGRES_W_PASSWORD=warehouse
POSTGRES_W_DB=warehouse
AIRFLOW__WEBSERVER__SECRET_KEY=<any long random string, keep stable>
AIRFLOW__CORE__FERNET_KEY=<output of: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())">